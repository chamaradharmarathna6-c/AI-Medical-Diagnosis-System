# Testing

Run `pytest -q` after local model training. The suite checks health/docs availability, registration and login behavior, authentication failures, local diabetes artifact prediction, request validation, workbook-backed doctors/recommendations, and SQL script/seed presence.
